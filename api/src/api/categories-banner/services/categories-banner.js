'use strict';

/**
 * categories-banner service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::categories-banner.categories-banner');
